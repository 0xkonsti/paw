/*
This is a simple "Hello, World!" program in Paw.
It demonstrates the basic syntax and structure of a Paw program.
*/

// here we define a fucntion named main with no parameters
// and it prints "Hello, world!" to the console
let main = || {
    let hello = "Hello, world!";
    println(hello);
};

// this is a Interpreter directive
// it tells the interpreter `from` where to start executing
from::main;
